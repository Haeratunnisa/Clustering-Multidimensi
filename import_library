# Make sure all required packages are installed. You can install it by running the following command in the RStudio console:
install.packages("packet_name")

# Here are some packages used in this project
library(cluster)
library(factoextra)
library(ggplot2)
